server:
  port: 80

#配置应用名称，这里用来注册eureka服务
spring:
  application:
    name: cloud-order-service

#配置Eureka
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/,http://eureka7002.com:7002/eureka/
